---
description: Show or update aios/router.md, the living map of my world
allowed-tools: Read, Write, Edit, Bash(ls:*), Bash(find:*)
---
Show the current `aios/router.md`. Reconcile it against what actually exists under `aios/` (projects, skills, brainstorm, connections, ledger, other_worlds) and propose precise updates so the map matches reality. Then apply them on my confirm.
